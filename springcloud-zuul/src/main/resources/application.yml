spring:
  application:
    name: springcloud-zuul
server:
  port: 8010
eureka:
  client:
    service-url:
      defaultZone: http://${eurekaHost}:8001/eureka
  instance:
    instance-id: ${spring.cloud.client.ip-address}:${server.port}
    prefer-ip-address: true
    ip-address: ${clientIp}

clientIp: localhost

eurekaHost: localhost

zuul:
  add-host-header: true
  routes:
    feign:
      # 配置路由，将/feign/**指向springcloud-consumer服务，
      # 在页面访问localhost:8010/feign/**相当于访问localhost:8003/**
      path: /feign/**
      service-id: springcloud-consumer
    ribbon:
      path: /ribbon/**
      service-id: springcloud-consumer-ribbon
    provider:
      path: /provider/**
      service-id: springcloud-provider
    auth:
      path: /system/**
      service-id: springcloud-system
  host:
    socket-timeout-millis: 60000
    connect-timeout-millis: 60000
  sensitive-headers:

ribbon:
  ReadTimeout: 10000
  ConnectTimeout: 10000

hystrix:
  command:
    default:
      execution:
        timeout:
          enabled: true
        isolation:
          thread:
            # 设置超时时间，如果设置小了，zuul第一次请求很容易报超时
            timeoutInMilliseconds: 60000

#security:
#  oauth2:
#    client:
#      access-token-uri: /system/oauth/token
#      user-authorization-uri: /system/oauth/authorize
#    resource:
#      user-info-uri: /system/auth/user
#      prefer-token-info: false

